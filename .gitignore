# Ignore everything by default
*

# Keep the ignore file itself
!.gitignore

# Only include files relevant to the Streamlit app deployment
!India-VOTE.py
!requirements.txt
!render.yaml
!README.md

# Data needed by the Streamlit app
!India_AC.geojson
!India_AC_hex.geojson

# Optional: allow a reference CSV if you want to commit it
# (comment this out if you prefer keeping data local)
#!eci_check_reports/
#!eci_check_reports/part_list_*.csv

# Everything else stays ignored to keep the repo lean

!eci_check_reports/
!eci_check_reports/part_list_S29_20250907_202919.csv

